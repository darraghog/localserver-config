# Stack __STACK__ — generated by scripts/add-service.sh
# Add a Caddy site in compose/tls-proxy/Caddyfile and run: ./scripts/deploy-stack.sh tls-proxy
services:
  app:
    image: __IMAGE__
    ports:
      - "__PUBLISHED_PORT__:__CONTAINER_PORT__"
    restart: unless-stopped
